function test(label, body) {
    if (!body()) {
        return console.log(`Failedd: ${label}`);
    }
    else {
        return console.log(`Success: ${label}`);
    }
}
function sqaured(x) {
    return x*x;
}
test("checking sqaure of 5", () => {
    return sqaured(5)=== 25;
});
